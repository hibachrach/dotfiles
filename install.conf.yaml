- defaults:
    link:
      relink: true
- clean: ['~']

- link:
  ~/.dotfiles: ''
  ~/.tmux.conf: tmux.conf
  ~/.nvim: config/nvim
  ~/.vim: vim
  ~/.zshrc:
    force: true
    path: zshrc
- shell:
  - [git submodule update --init --recursive, Installing submodules]
